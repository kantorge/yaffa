# Test Coverage: Precision Improvements

| Use case | Rule (doc) | Expected behavior (+ deny case) | Evidence | Type | Status |
|---|---|---|---|---|---|
| `MoneyCast::get()` returns a `Brick\Money\Money` at the column's own scale | `architecture.md` "Which Models/Fields" | Hydrated attribute is `Money`, amount string matches column scale (e.g. `42.5000` for a scale-4 field) | `MoneyCastTest::test_get_returns_a_money_instance_at_the_columns_scale` | unit | existing |
| `MoneyCast` resolves currency via the model's own `resolve*Currency()` method, not a fixed relation | `architecture.md` "per-model currency resolver" | `TransactionItem::amount` resolves the parent transaction's currency; `Account::opening_balance` resolves its own `currency_id`; `AccountMonthlySummary::amount` resolves the account's currency when `account_entity_id` is set, and falls back to the user's base currency when it's null (generic/account-agnostic budgets) | `MoneyCastTest::test_get_resolves_the_parent_transactions_currency`, `test_account_opening_balance_round_trips_and_serializes_as_a_string`, `test_account_monthly_summary_amount_uses_the_accounts_currency`, `test_account_monthly_summary_amount_falls_back_to_base_currency_without_an_account` | unit | existing |
| `MoneyCast::set()`/`DecimalCast::set()` accept a plain scalar and round `HalfUp` to the column's exact scale on write | `architecture.md` "round with `RoundingMode::HalfUp` on write" | A float/string input is stored at-scale (e.g. `12.3` → `12.3000`) | `MoneyCastTest::test_set_accepts_a_plain_scalar_and_stores_it_at_the_exact_scale`, `DecimalCastTest::test_set_accepts_a_plain_scalar_and_stores_it_at_the_exact_scale` | unit | existing |
| `MoneyCast`/`DecimalCast::serialize()` emits a decimal **string**, not a JSON number | `architecture.md`/`flows.md` Flow 1 step 3 — the core wire-format break | `toArray()`/`toJson()` produce `"7.2500"` (string), not `7.25` (number), for both a `Money`-cast and a `BigDecimal`-cast field | `MoneyCastTest::test_serialize_emits_a_decimal_string_not_a_float`, `DecimalCastTest::test_serialize_emits_a_decimal_string_not_a_float` (both assert `assertIsString` + exact string content + absence of the bare-number form in raw JSON) | unit | existing |
| Nullable `Money`-cast field round-trips as `null` | `architecture.md` field table (`dividend` nullable) | A transaction with no dividend leg loads with `config->dividend === null`, no cast exception | `MoneyCastTest::test_nullable_money_field_round_trips_as_null` | unit | existing |
| `DecimalCast::get()` returns a `Brick\Math\BigDecimal` at the column's scale (ratio, not a currency amount) | `architecture.md` `CurrencyRate.rate` row | `CurrencyRate::rate` hydrates as `BigDecimal`, e.g. `'1.5'` → `'1.5000000000'` | `DecimalCastTest::test_get_returns_a_big_decimal_at_the_columns_scale` | unit | existing |
| Combining `Money` of different currencies throws rather than silently producing a wrong total | `architecture.md` "Legacy-data currency mismatch" — the guard FR-5 relies on | `Money::plus()` throws `MoneyMismatchException` for mismatched currencies (the brick/money behavior the mismatch-handling code below depends on) | `MoneyCastTest::test_combining_money_of_different_currencies_throws` | unit | existing |
| `TransactionApiController::convertToBase()` — `Money::convertedTo(..., RoundingMode::HalfUp)` replaces the old `amount * rate` float multiplication | `architecture.md` "Changed" — `convertToBase()`; `flows.md` doesn't cover this internal helper directly | Matches the expected product for a representative rate; a tie at the smallest decimal place rounds away from zero (`HalfUp`, not to-even or truncated); the source amount's own scale (not the base currency's) is reused for the result | `TransactionApiControllerConversionTest` (3 tests: representative rate, tie-rounding, scale reuse) | unit | existing |
| `App\Support\ScheduleInstance::toArray()` unwraps `Money`/`BigDecimal` the same way the real casts' `serialize()` does, not `Money`'s own `JsonSerializable` shape | `architecture.md` "the one non-Eloquent value object... verified, not assumed" | `cashflow_value` serializes as `(string) $money->getAmount()` (e.g. `'123.4500'`), `quantity` as `(string) $bigDecimal` (e.g. `'10.0000'`), and a `null` cashflow_value stays `null` — matching `MoneyCast`/`DecimalCast`'s wire shape rather than `{"amount":...,"currency":...}` | `tests/Unit/Support/ScheduleInstanceTest.php` (3 tests) | unit | existing |
| Report endpoints (`budgetChart`, `getCategoryWaterfallData`, `getCashflowData`) deliberately keep the pre-feature `float` JSON wire format, unlike transaction/investment/account endpoints | `architecture.md`/`flows.md` "Report endpoints are the deliberate exception" | Numeric chart values remain directly float-comparable in the JSON response | `ReportApiBudgetChartTest`'s existing `assertEqualsWithDelta(-50.0, $entry['forecast'], 0.001)`-style assertions (see `budget-schedule-redesign/tests.md`) exercise this incidentally — no test explicitly asserts `assertIsFloat`/rejects a string here | integration | existing (indirect) |

## Existing coverage

`MoneyCastTest` and `DecimalCastTest` (both `tests/Unit/Casts/`) are the direct, focused coverage for this feature's core mechanism: get/set/serialize round-tripping at the correct scale, currency resolution across all the branching cases the codebase actually has (parent-transaction relation, own-column relation, conditional account-vs-base-currency fallback), and the decimal-string wire format that is this feature's headline breaking change. `TransactionApiControllerConversionTest` pins the one piece of application logic this feature rewrote outright (`convertToBase()`), including the specific rounding-tie behavior a naive `amount * rate` float multiplication would have gotten wrong non-deterministically. `ScheduleInstanceTest` closes the loop on the one place `architecture.md` flagged as a manual, easy-to-miss re-implementation of the serialization contract outside Eloquent's own cast machinery.

This is solid, well-targeted unit coverage for a change that is entirely about type/serialization correctness rather than business logic or authorization (see `permissions.md`'s n/a note) — every cast, every currency-resolution branch, and the one non-Eloquent value object doing its own serialization all have a passing test today.

## Recommended CI gate

All coverage above is `tests/Unit/` — no DB fixtures beyond `RefreshDatabase`/factories, no external services. Already belongs in, and runs in, the standard suite:

```yaml
# .github/workflows/tests.yml (illustrative — adapt to the repo's actual CI file/runner)
- name: Run test suite
  run: vendor/bin/sail artisan test --compact
```

## Gaps — currently unverified

1. **`TransactionService::getInvestmentConfigCashFlow()`'s legacy currency-mismatch handling (`app/Services/TransactionService.php:189-205`) has no test.** This is the one place `architecture.md`'s "Known Risks" section calls out as deliberately not throwing: when an investment transaction's `price` (investment currency) and `commission`/`tax`/`dividend` (account currency) legs no longer share a currency — because the account or investment's currency was changed after the transaction was recorded — the method logs a `warning` (`"Investment transaction cash flow spans mismatched currencies (legacy data)"`, with the transaction id) and returns `null` instead of letting `Money::plus()`'s `MoneyMismatchException` escape. `tests/Unit/Services/TransactionServiceTest.php` has cash-flow tests for withdrawal/deposit/transfer/buy/sell/dividend, but none constructs a mismatched-currency investment transaction to exercise this branch. Proposed test: build an investment transaction whose `config` legs are `Money` instances in two different currencies (e.g. by changing the account's `currency_id` after the transaction detail row was created, bypassing `TransactionRequest::accountInvestmentCurrencyMatchRule()`'s create-time guard), call the cash-flow computation, and assert it returns `null` without throwing (optionally also asserting the warning is logged, via `Log::shouldReceive('warning')->once()`).
2. **The frontend `config.price` precision-truncation risk (`resources/js/shared/lib/helpers/index.js:198`, `processTransaction()`) is real, documented as still-open in `frontend-precision-followup.md`, and has no test on either side.** `architecture.md`'s Known Risks section confirms by code inspection that a `DECIMAL(20,10)` investment price fetched from the API can silently lose precision when converted to a JS `Number` and re-saved without the user touching the price field. This is a JS-side numeric-precision bug, not a PHP unit-testable rule — no backend test can pin it, and no frontend test currently does either. Flagged here rather than silently dropped since it's this feature's one confirmed still-open correctness gap; the fix (and its test) belongs with whoever picks up the follow-up doc, likely a JS test asserting a round-trip through `processTransaction()`/its inverse preserves all 10 fractional digits of a representative high-precision price.
3. **No test exercises `MoneyCast`/`DecimalCast::set()`'s over-precise-input tolerance directly** (`architecture.md`: `RoundingMode::HalfUp` on write "tolerates over-precise input until real input clamping exists end-to-end" — an open, acknowledged gap, not a bug). `MoneyCastTest::test_set_accepts_a_plain_scalar_and_stores_it_at_the_exact_scale` only exercises a value already at-scale (`12.3` → a scale-4 column). Proposed test: write a value with more fractional digits than the column supports (e.g. `12.34567` into a scale-4 `amount` field) and assert it's silently rounded to `12.3457`, not rejected — pins the current (intentionally permissive) behavior so a future tightening of this rule is a deliberate test change, not a silent regression either way.
